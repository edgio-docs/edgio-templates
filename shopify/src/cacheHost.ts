/**
 * Set this to the hostname of the XDN site when you're not running
 * the origin site behind the XDN in production.  Doing so will
 * send all prefetch requests to the XDN instead of the origin site.
 */
export default undefined
