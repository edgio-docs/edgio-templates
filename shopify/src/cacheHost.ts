/**
 * Set this to the hostname of Layer0 site when you're not running
 * the origin site behind Layer0 in production.  Doing so will
 * send all prefetch requests to Layer0 instead of the origin site.
 */
export default undefined
